{
  "name": "example",
  "private": true,
  "version": "0.0.1",
  "description": "example",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "node server.js",
    "start:frontend": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.5.2",
    "@stencil/core": "^2.3.0",
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "@capacitor-community/http": "file:..",
    "@capacitor/cli": "^3.0.0-alpha.9",
    "@capacitor/core": "^3.0.0-alpha.9"
  }
}
